project: mlops_project
registry: data/registry.db # Path to the Feast registry file, relative to feature_store/
provider: local
online_store:
    type: sqlite
    path: data/online_store.db # Path to the online store database, relative to feature_store/
offline_store:
    type: file